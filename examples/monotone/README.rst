.. _general_examples/monotone:

Monotone Radix Heaps
-----------------------

This gallery shows some basic uses of the monotone radix heaps, whose keys
must be removed in nondecreasing order -- exactly the access pattern
Dijkstra's algorithm needs.
